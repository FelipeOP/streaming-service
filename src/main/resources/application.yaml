spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/streamings
    password: root
    username: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

      
logging:
  level:
    root: INFO
