spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/movies
    password: root
    username: postgres
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

logging:
  level:
    root: INFO
